Installation
============

``pycircdb`` is distributed on PyPI and can be installed with ``pip``:

.. code-block:: bash

   pip install pycircdb

Requirements
------------

* Python 3.10 or newer

To verify the installation was successful, run:

.. code-block:: bash

   pycircdb --help